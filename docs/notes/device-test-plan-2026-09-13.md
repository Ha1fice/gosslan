# 真机测试计划（2026-09-13）· 面向「BLE 加入 mesh 稳定性 · 聊天高效 · 手机↔手机 mesh」

> 目标：**每一测都给出"看哪一个日志/数字"**，避免"感觉不对"这种没法定位的反馈。
> 日志统一从：设置 → 运行日志 → **先点「复制全部」再粘贴**（比截图强得多）。
> macOS 落盘：`~/Library/Application Support/com.gosslan.app/logs/`；Android：`adb logcat -s gosslan:I`。
> 本机无法自测的射频行为只能靠这张表，请**按顺序**做；T1/T2 是一切的前提。

## 0. 准备

- **用 `release-artifacts/` 里最新的那份包**（每次修完我都会留一份，文件名带版本号），
  并在报告里写上你装的版本号 —— 下面的判读方法**不随版本变**，但"旧版会失败"的项要按新版判。
  写这份文档时的基线是 `4.3.2`（Android `gosslan-4.3.2-arm64-v8a-release.apk`、
  macOS `gosslan-4.3.2-aarch64-apple-darwin.app.zip`）；**更新版本请以目录里最新文件为准**。
- 出包命令：`npm run dist`（mac 上会**并行**出 mac + 安卓；Windows 上只出当前环境那一个）。
- 建议设备组合：Mac（desktop）+ 安卓手机；能凑第二台安卓更好（T5/T6）。
- 每次测试前记下开始时间，日志按时间切。

> **修订记录（第 2 版，2026-09-13）**：T3 / T6 / T7 的"已知会失败"结论**已经过期** ——
> 那三条对应的缺陷在 4.3.0 里已经修掉（提交 `324fa8f`），现在它们变成**验证项**：
> 如果你看到旧版描述的那种失败，那就是**回归**，请立刻告诉我。
> 另外新增 T10 / T11（4.3.1、4.3.2 修的两个交互问题，顺手验一下即可）。

---

## T1【最高优先】BLE 链路能不能活过 45 秒

**背景**：审计发现 BLE 读循环漏了回灌读活性 ⇒ 健康链路 15s 判不健康、45s 被看门狗拆掉。
本轮已修，需要你确认"真的不再自拆"。

**步骤**
1. 手机与 Mac 只开蓝牙（关 Wi-Fi），互相加好友、建立会话。
2. 保持两端都在前台，**什么都别做，静置 3 分钟**。
3. 第 2 分钟时，互相发一条消息。
4. 静置结束后，再互相发一条消息。

**期望**
- 3 分钟内**不应出现** `-conn peer=… 读活性超过 45s 无入站帧 ⇒ 拆除死链路`（这是"自拆"的标志）。
- 第 2 分钟那条消息应**立即送达**（不应等几十秒）。
- 链路徽标应一直显示"蓝牙直连"，不应时有时无。

**要贴给我**
- 两边日志里所有 `mesh` 与 `ble` 行（尤其 `[GATT]` / `-conn` / `[SEND]` / `[RECV]` / `[FRAG]`）。
- 如果第 2 分钟的消息延迟 > 2s，请贴时间戳。

---

## T2【最高优先】BLE 到底是什么速度（MTU 基准）

**背景**：文档里的"1KB/s、MTU=23"是**猜的**。本轮加了两条日志，可以一次测准。

**步骤**
1. 连上 BLE 后，在日志里搜 `[GATT] MTU 协商结果` 与 `[GATT] 外设侧 MTU 协商结果`。
2. 发一条**纯文字**消息（例如 10 个字），记录两端 `[SEND] … bytes=… 分片=n` 的 `bytes` 与 `分片`。
3. 发一张 **100~200KB 的图片**，秒表记「本机开始发送 → 对端看到图片」的墙钟时间。
4. 再发一张 **1MB 左右的大图**，同上（**很可能失败，见 T3，失败也是有效结论**）。

**期望 / 我要算的数**
- `每片有效载荷`（两条 MTU 日志）；
- 文字消息的 `分片数 ÷ 耗时` ⇒ 每片实际间隔；
- 图片的 `字节数 ÷ 耗时` ⇒ 真实 KB/s。

**要贴给我**
- 两条 MTU 日志原文；文字消息的 `[SEND]` 行；两次图片传输的起止时间与结果（成功/卡在"发送中"/报错文案）。

> 有了这三个数，我才能判断瓶颈是"MTU 没协商上"还是"每片 12ms 的节流"，
> 并据此决定要不要动 `NOTIFY_CHUNK_INTERVAL` 和主动 `requestMtu`。

---

## T3【高】大文件到底能不能传完（**已修，请你确认**）

**背景（旧结论已过期）**：以前 `file.rs` **固定 30s** 等 `FileCompleteAck`，而 1MB 的 256 个分块
在 1s 内就全部入队、`FileDone` 排在很后面 ⇒ 必然超时 ⇒ 每 5s 从头重传、`seq` 从 0 重来
⇒ 接收方报**「文件分片顺序错误」**。4.3.0（`324fa8f`）已改成**进展驱动**：
只要还有分块真的在往链路上写，就一直等；**只有安静 30s 没有任何写出进展**才算失败。
另外两处也修了：重传时接收方**丢掉旧状态重新开始**（不再回"重复的文件传输"）、
**迟到/重复的分片直接忽略**（不再整单失败）。

**步骤**：BLE 直连下依次发 **50KB / 200KB / 1MB** 三个文件（图片或文档都行），每个都要**等到出结果**
（成功或明确失败），别中途取消。

**期望（4.3.2）**
- 三个都**应当最终传完**（对端能看到文件、能打开；发送方不再卡在"发送中"）。
- 耗时随体积线性增长（BLE 慢是正常的，见 T2 算出真实 KB/s）；**1MB 可能要几分钟，请耐心等完**。
- 日志里**不应再出现** `文件分片顺序错误`。
- 只有**真断链/真丢包**才会失败，那时会看到 `接收方未确认文件完成`（发送方）——
  这条出现才算真失败，请把前后 20 行日志贴给我。

> 这一条现在是"确认修复"，不是"复现 bug"。传完了 = 修复有效；传不完 = 回归，请把日志给我。

---

## T4【高】手机 ↔ 电脑 mesh（BLE-only，关 Wi-Fi）

**步骤**：关掉两边 Wi-Fi → 只靠蓝牙 → 互发文字 / 图片 / 语音（若有）→ 断一次再连。

**要看**：`[GATT]` 握手是否一次成功；断链后**多久**重连上（记秒表）；重连期间发的消息是否最终到达。

---

## T5【高】手机 ↔ 手机（两台安卓，BLE-only）

**步骤**：两台安卓只开蓝牙 → 互相搜索 → 加好友 → 互发消息 → 各退到后台 1 分钟再回来 → 再互发。

**已知边界**（不是 bug，如实记录程度即可）：Android 后台扫描被系统限流，
Rust 侧后台节奏是 30s；被系统杀掉则不扫描。

**要看**：加好友能否成功；`[SCAN] 收到 N 个广播，其中 M 个是本应用服务` 的 M 是否 ≥1。

---

## T6【中】三节点中继（手机A → Mac → Windows/另一台手机）

**背景**：多跳"最窄路径"（3 节点、都同时在线、单聊、默认中继策略）代码上应能通，但**从未验收**。

**步骤**：A（手机，BLE）↔ B（Mac，BLE）↔ C（Windows 或第二台手机，LAN）。
A 与 C 不在彼此射程/网段内 → A 给 C 发消息。

**期望**：C 能收到（B 只做转发）。
**群聊（重点！已修，请单独测一遍）**：以前"非成员中继不转发群消息"⇒ BLE-only 三点中继里
**群聊永远不通、而同链路单聊正常**；4.3.0（`324fa8f`）已把判据改成"只决定要不要本地消费"，
非成员照样转发。现在请**建一个含 A 与 C 的群**、在群里发消息，确认 C 能收到；
若仍不通，请把 B 的日志贴出来（转发决策那几行）。

**要贴给我**：B 的日志里有没有 `+conn`/转发相关行；A 上消息状态是否变成"已送达"；
群聊那条也照同样格式给一份。

---

## T7【中】断链恢复（"加入 mesh"体感）

**步骤**：连上 BLE 后 → 关掉手机的蓝牙 → 等 10s → 再打开 → 记「重连成功」的秒数。重复 3 次。

**期望（4.3.2）**：4.3.0 起 `teardown_link` 会**立刻** `wake_scan` 并清掉该地址的失败退避，
所以断链后应当是**秒级**重连（不再是"等下一轮扫描，后台最长 30s"）。
请记下三次的秒数；若有一次超过 10s，把那段日志贴给我（看是不是扫描窗口/退避在挡）。

---

## T8【必做】局域网回归不能被搞坏

老规矩：同一 Wi-Fi 下 Mac ↔ Windows 的正常收发/图片/文件/群聊，
以及 `bash scripts/e2e-dev.sh`（单机协议级 E2E，跑前请先关掉正在运行的 dev 实例）。

---

## T10【顺手验，低优先】蓝牙开关的"手感"（4.3.1 修的）

**背景**：以前点蓝牙开关要干等 2~3 秒开关才动（前端在等后端，后端在等 CoreBluetooth）。
现在开关是**乐观更新**：先按你的意图动，再去后端启停；失败才回退并提示。

**步骤**
1. 设置 → 网络 → 点「蓝牙」开关（**关**）→ 再点一次（**开**）。
2. 稳定后，**1 秒内连点两下**（关→开）。
3. 故意制造失败（可选）：Android 上把「附近的设备」权限拒掉后点开。

**期望**
- 开关**当帧就滑过去**，滑杆上出现一枚小转圈；**最多 2~3 秒**转圈消失。
- 连点两下后，开关**停在最后一次的选择上**（= 开），不会"点了没反应"。
- 权限被拒时：开关先动 → 然后**回退**成原样 + 一条错误提示（这就是"失败回退"，不是 bug）。

**判读（日志里搜这几条）**
| 日志 | 含义 |
|---|---|
| `蓝牙通道切换：已停止 → 开启` | 一次**真实**启停（每次点击最多一条） |
| `蓝牙通道开关进入冷却：等待 Nms 后执行（合并抖动）` | 第二次点击被**排队**（不是丢弃）——连点测试应当看到这条 |
| `运行状态已经是目标状态（幂等）` | 重复请求被幂等挡掉（没有多拆一次蓝牙栈） |
| `忽略高频蓝牙通道切换请求` | ⚠️ **旧行为**：看到它就说明你装的不是新包（≤4.3.0） |

---

## T11【顺手验，低优先】安卓长按气泡弹复制/转发面板（4.3.2 修的）

**背景**：以前"按在文字上长按不弹"（判据被 `.gosslan-selectable` 挡掉）、
"弹出来一放手就缩回去"（HeadlessUI 把抬手指当成点了外面）。两个都修了。

**步骤**
1. 在**气泡正文的文字上**按住不动约 0.5 秒 → 松手。
2. 再分别试：气泡内边距（文字外那圈）、表情图片上、`@提及` 上。
3. 面板弹出后点「复制」「转发」各一次；再点面板外的遮罩一次。

**期望**
- 按住约 0.5 秒时有**一下触觉反馈**，面板弹出；**松手后面板留着**（不缩回去）。
- 上述四种落点都**能弹出**。
- 「复制」有"已复制"提示；「转发」打开转发弹窗；点遮罩能正常关掉面板。
- 面板里的「选择文字」仍然可用（进入后正文自动全选、弹出系统复制工具条）。

**判读**：这条纯前端，**没有日志**，只看行为。
若出现"松手就缩回"或"按文字不弹"，说明包是旧的（≤4.3.1）。

---

## 报告模板（照抄填写）

```
【T?】步骤：… 
预期：…
实际：…
日志片段（贴原始行，含时间戳）：…
```

## 本轮明确**不需要**你测的（按你的优先级靠后）

- Windows 端蓝牙（代码里就没有外设角色，只有能拨的 central）。
- 各种链接样式 / UI 交互细节。

---

## T9【Mac 专项】鼠标拖选文字选不中（用户 2026-09-13：Mac + **纯文字**也会）

已排除的两个原因（所以不用再怀疑它们）：
- **不是表情图片被拖走** —— 纯文字消息同样复现（用户已确认）。
- **不是内边距锚点** —— 气泡根已加 `select-text`（上一轮修复），仍是同样表现。

剩下的可能只剩两类：**① 全局层面的 WebView/系统行为；② 我们这个页面的 DOM 结构**。
下面两步能把范围**一刀切开**，请按顺序做。

### 第一步（不用重新打包，1 分钟）

在**同一个 app、同一台 Mac**上，分别尝试用鼠标拖选：

1. **运行日志**窗口里的一行日志；
2. **设置 → 关于**里的设备指纹；
3. 一条长文本消息 → 点「展开」→ 在**弹窗**里拖选那段文字；
4. （对照）聊天区气泡正文 —— 已知失败。

**判读**
- 若 1/2/3 **有一个能选中** ⇒ 问题只在我们**消息行**这块 DOM（我继续按 DOM 方向查，
  重点查虚拟列表的行容器与 `user-select` 继承）；
- 若 1/2/3 **全都选不中** ⇒ 是 **WKWebView / Tauri 全局**层面的问题（下一步）。

### 第二步（需要重新打包一次，验证一个具体假设）

最可疑的全局因素是 `src-tauri/tauri.conf.json` 里的 `"dragDropEnabled": true`
（wry 会把 WKWebView 注册成 `NSDraggingDestination`，可能与 WebKit 自己的选区拖拽打架）。

我已经准备好一份**只改这一个开关**的覆盖配置（没有改主配置，随时可丢）：

```bash
cd src-tauri
CARGO_HOME="$PWD/target/cargo-home" \
  npx tauri build --features bluetooth --target aarch64-apple-darwin --bundles app \
  --config tauri.nodrop.conf.json
bash ../scripts/pack-macos-app.sh aarch64-apple-darwin
```

装上这个包后再拖选一次：

- **能选中了** ⇒ 确认是 `dragDropEnabled` 的副作用。接下来是一个取舍：
  关掉它就**失去"把文件拖进聊天框发送"**（那是 Tauri 原生拖放提供的真实路径）。
  可以做的折中：默认关、在设置里给个开关；或保留原生拖放但另找选区方案。
- **还是选不中** ⇒ 那就是 macOS/WKWebView 层面的（已知 macOS 15 有一条选区回归），
  我会转去查系统版本 + wry 版本的已知问题，不再在 CSS/DOM 上耗。

> 把这两步的结果贴给我即可（第一步只要一句"1/2/3 哪个能选中"）。
